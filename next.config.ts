import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // output: 'export', // 👈 ini kunci agar bisa jadi static site
  // reactStrictMode: true,
  // Tambahkan config lain jika perlu
}

export default nextConfig
